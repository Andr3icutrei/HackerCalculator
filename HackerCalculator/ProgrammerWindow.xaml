<Window x:Class="HackerCalculator.ProgrammerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HackerCalculator"
        mc:Ignorable="d"
        Title="ProgrammerMode" Height="535" Width="336">
    <Window.DataContext>
        <local:ButtonsProgrammerViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="4" />
            <Setter Property="Foreground" Value="#FF20C20E" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="BorderBrush" Value="#FF20C20E" />
            <Setter Property="Background" Value="Black" />
        </Style>
        <Style TargetType="TextBox" x:Key="ComboBoxTextBoxStyle">
            <Setter Property="Background" Value="Black" />
            <Setter Property="Foreground" Value="#FF20C20E" />
            <Setter Property="BorderBrush" Value="#FF20C20E" />
        </Style>
    </Window.Resources>

    <Grid Background="Black">

        <Grid Margin="0,291,0,0">
            <ItemsControl Name="MatrixButtons" ItemsSource="{Binding ButtonsData}" Margin="0,20,0,0">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ItemsControl ItemsSource="{Binding}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                    </Grid>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="Grid.Column" Value="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}"/>
                                </Style>
                            </ItemsControl.ItemContainerStyle>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Content}" IsEnabled="{Binding IsEnabled}"
                        Click="Button_Click"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>

                            <ItemsControl.AlternationCount>4</ItemsControl.AlternationCount>
                        </ItemsControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                        </Grid>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Grid.Row" Value="{Binding RelativeSource={RelativeSource Self}, Path=(ItemsControl.AlternationIndex)}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>

                <ItemsControl.AlternationCount>5</ItemsControl.AlternationCount>
            </ItemsControl>
        </Grid>

        <Grid Margin="0,215,0,215">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="166*"/>
                <ColumnDefinition Width="52*"/>
                <ColumnDefinition Width="119*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>

            </Grid.RowDefinitions>
            <TextBox Name="TextBoxResult" Grid.Row="1" Background="Black" Margin="4,1,4,4" FontSize="20" Foreground="#FF20C20E" BorderBrush="#FF20C20E" Grid.ColumnSpan="3" 
             TextAlignment="Right" Text="Result: " IsReadOnly="True"/>
            <TextBox Name="TextBoxCalculation" Margin="4,-2,4,0" Background="Black"
             FontSize="20" Foreground="#FF20C20E" BorderBrush="#FF20C20E" Grid.ColumnSpan="3" TextAlignment="Right" IsReadOnly="True" Height="40" VerticalAlignment="Top"/>

        </Grid>
        <Grid Margin="10,10,179,414">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Button Content="Change &#xD;&#xA;to Standard" 
             Click="ChangeWindow_Click" Margin="4,4,4,0"/>
        </Grid>


        <Grid Background="Black" Margin="10,92,164,314">
            <ListBox x:Name="listBoxFromBase" 
                     ItemsSource="{Binding BaseItems}" 
                     SelectedItem="{Binding SelectedFromBaseItem, Mode=TwoWay}"
                     HorizontalAlignment="Left" Width="158"
                     Background="Black" Foreground="#FF20C20E"
                     BorderThickness="0" Margin="0,30,0,-23">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <RadioButton Content="{Binding}"
                     GroupName="FromBaseGroup"
                     Foreground="#FF20C20E"
                     Margin="2,2,2,2" 
                     BorderBrush="#FF20C20E" 
                     BorderThickness="1"
                     IsChecked="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSelected}"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            <Label Content="From base:" Foreground="#FF20C20E" HorizontalAlignment="Left" Margin="0,4,0,0" VerticalAlignment="Top" Width="112"/>
        </Grid>
        
        <Grid Margin="187,92,10,304">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="41*"/>
                <ColumnDefinition Width="98*"/>
            </Grid.ColumnDefinitions>
            <ListBox x:Name="listBoxToBase"  
         ItemsSource="{Binding BaseItems}" 
         SelectedItem="{Binding SelectedToBaseItem, Mode=TwoWay}"
         HorizontalAlignment="Left" Width="136"
         Background="Black" Foreground="#FF20C20E"
         BorderThickness="0" Grid.ColumnSpan="2" Margin="0,35,0,-14">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <RadioButton Content="{Binding}"
                             GroupName="ToBaseGroup"
                             Foreground="#FF20C20E"
                             Margin="2,2,2,2" 
                             BorderBrush="#FF20C20E" 
                             BorderThickness="1"
                             IsChecked="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=IsSelected}"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
            <Label Content="To base:" Foreground="#FF20C20E" HorizontalAlignment="Left" Margin="5,4,0,0" VerticalAlignment="Top" Width="94" Grid.ColumnSpan="2"/>
        </Grid>
    </Grid>


</Window>
